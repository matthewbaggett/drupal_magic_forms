<?php if (strlen($field->get_label()) > 0): ?>
  <div class="label">
    <label for="<?php echo magic_forms_mangle_to_css_class($field->get_name()); ?>">
      <?php echo $field->get_label(); ?>
    </label>
  </div>
<?php endif; ?>