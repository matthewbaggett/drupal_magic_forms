<?php require("form.label.phtml"); ?>

<select name="<?php echo $field->get_name(); ?>">
  <?php if(count($field->get_options()) > 0): ?>
    <?php foreach($field->get_options() as $key => $option_label): ?>
      <option value="<?php echo $key; ?>"><?php echo $option_label; ?></option>
    <?php endforeach; ?>
  <?php endif; ?>
</select>